#pragma once

// the scenario we are playing the game under
enum PlayMode{
    OFFLINE, // we are playing offline
    JOINING, //this client is joining a server
    HOSTING, //this client is hosting a server
};
